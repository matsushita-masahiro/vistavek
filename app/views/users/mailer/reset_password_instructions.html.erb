<p>Hello <%= @resource.email %>!</p>

<p>パスワードを変更するためのリンクを誰かがリクエストしました。これは、以下のリンクから実行できます.</p>

<p><%= link_to 'パスワード変更する', edit_password_url(@resource, reset_password_token: @token) %></p>

<p>リクエストしていない場合は、このメールを無視してください</p>
<p>上記のリンクにアクセスして新しいパスワードを作成するまで、パスワードは変更されません。</p>
